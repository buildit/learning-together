import React, { Component } from "react";

export default class Navbar extends Component {
  render() {
    return (
      <nav>
        <a href="foo.html">Logo</a>
        <div>Search bar</div>
        <a href="foo.html">Login</a>
      </nav>
    );
  }
}
